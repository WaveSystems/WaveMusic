!!!
%html
  %head
    %title WaveMusic
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .navbar.navbar-inverse.navbar-static-top
      .navbar-inner
        .container
          %a{ class: "brand", href: "#{root_path}"} Wave music
          %ul.nav.pull-right
            - if user_signed_in?
              %li= link_to "Hello! #{current_user.username}", :root
              %li= link_to "Sign out", :destroy_user_session, method: 'delete'
            - else
              %li= link_to "Sign in", :new_user_session
              %li= link_to "Sign up", :new_user_registration

    = yield

    %footer.footer
      %center
        %p 2012 WaveSystems
